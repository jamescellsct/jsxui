# Variants

Each component has a set of props that controls the behavior of the component. Variants are the reusuable configurations of props that can be used to create different implementations of components.
